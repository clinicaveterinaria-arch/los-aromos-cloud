# Los Aromos Cloud V1

Primera versión funcional para Render + PostgreSQL.

Usuario inicial: `admin`  
Clave inicial: `losaromos`

## Render
Build Command:
`pip install -r requirements.txt`

Start Command:
`uvicorn app.main:app --host 0.0.0.0 --port $PORT`
